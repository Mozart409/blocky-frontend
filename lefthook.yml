pre-commit:
  parallel: true
  commands:
    build:
      run: pnpm run build
    just-fmt:
      run: just --fmt
      stage_fixed: true
pre-push:
  commands:
    image:
      run: pnpm run image-amd
    actionlint:
      run: actionlint
